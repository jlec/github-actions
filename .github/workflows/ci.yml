---
name: Continuous Integration

# yamllint disable-line rule:truthy
on:
  # yamllint disable-line rule:empty-values
  workflow_dispatch:

  # yamllint disable-line rule:empty-values
  pull_request:

  push:
    branches:
      - "*"
    tags-ignore:
      - "**"

permissions: read-all

jobs:
  ##############################################################################
  #
  # Run on everything but Releases and WIP
  #
  ##############################################################################
  linting:
    if: >-
      !startsWith(github.event.head_commit.message, 'bump:') &&
      !startsWith(github.event.head_commit.message, 'WIP')
    name: "Run Linting against all files"
    uses: reusable-linting.yml@main

  dependabot:
    name: "Handle dependabot PR"
    if: >-
      ${{ github.actor == 'dependabot[bot]' }} &&
      !startsWith(github.event.head_commit.message, 'bump:') &&
      !startsWith(github.event.head_commit.message, 'WIP')
    needs:
      - linting
    uses: reusable-pr-dependabot.yml
    permissions:
      contents: write
      pull-requests: write
    secrets:
      token: ${{ secrets.GITHUB_TOKEN }}

  ci-generic:
    name: "Run generic CI"
    if: >-
      ${{ github.actor != 'dependabot[bot]' }} &&
      !startsWith(github.event.head_commit.message, 'bump:') &&
      !startsWith(github.event.head_commit.message, 'WIP')
    needs:
      - dependabot
      - linting
    uses: reusable-ci-generic.yml
    permissions:
      contents: write
      pull-requests: write
    secrets:
      pat: ${{ secrets.PAT_GITHUB_TOKEN }}
      token: ${{ secrets.GITHUB_TOKEN }}
